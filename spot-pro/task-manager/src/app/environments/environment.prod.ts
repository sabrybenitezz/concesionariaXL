// src/environments/environment.prod.ts
export const environment = {
  production: true,   // Este es el valor para el entorno de producción
  apiBaseUrl: 'https://miapp.com/api'  // Cambia esta URL por la de tu API en producción
};
